/// <reference types="react" />
import { FieldInterface } from './fields/types';
declare const Field: {
    ({ required, children, name, type, label, error, placeholder, options, strength, className, defaultValue, displayProperty, tooltip, tooltipBackground, tooltipColor, tooltipIconColor, }: FieldInterface): JSX.Element;
    defaultProps: {
        defaultValue: any;
        options: any[];
        tooltip: string;
        tooltipBackground: string;
        tooltipColor: string;
        tooltipIconColor: string;
    };
};
export default Field;
